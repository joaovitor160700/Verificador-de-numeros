# Verificador-de-numerosgit init
git add .
git commit -m "Primeiro commit - adicionando código"
git branch -M main
git remote add origin https://github.com/seu-usuario/verificador-numeros.git
git push -u origin main
